/*
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef EIGENPY_CONFIG_HH
# define EIGENPY_CONFIG_HH

// Package version (header).
# define EIGENPY_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define EIGENPY_VERSION "3.12.0"
# define EIGENPY_MAJOR_VERSION 3
# define EIGENPY_MINOR_VERSION 12
# define EIGENPY_PATCH_VERSION 0

#define EIGENPY_VERSION_AT_LEAST(major, minor, patch) (EIGENPY_MAJOR_VERSION>major || (EIGENPY_MAJOR_VERSION>=major && \
                                                             (EIGENPY_MINOR_VERSION>minor || (EIGENPY_MINOR_VERSION>=minor && \
                                                                                                     EIGENPY_PATCH_VERSION>=patch))))

#define EIGENPY_VERSION_AT_MOST(major, minor, patch) (EIGENPY_MAJOR_VERSION<major || (EIGENPY_MAJOR_VERSION<=major && \
                                                            (EIGENPY_MINOR_VERSION<minor || (EIGENPY_MINOR_VERSION<=minor && \
                                                                                                     EIGENPY_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
//
// Explicit template instantiation on Windows need to add
// dllexport on the definition and dllimport on the declaration.
// The XXX_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI macro
// should be set on the declaration while
// the XXX_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI macro
// should be set on the definition.
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define EIGENPY_DLLIMPORT __declspec(dllimport)
#  define EIGENPY_DLLEXPORT __declspec(dllexport)
#  define EIGENPY_DLLLOCAL
#  define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT __declspec(dllimport)
#  define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#  define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT __declspec(dllimport)
#  define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT __declspec(dllexport)
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define EIGENPY_DLLIMPORT __attribute__ ((visibility("default")))
#   define EIGENPY_DLLEXPORT __attribute__ ((visibility("default")))
#   define EIGENPY_DLLLOCAL  __attribute__ ((visibility("hidden")))
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT __attribute__ ((visibility("default")))
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT __attribute__ ((visibility("default")))
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define EIGENPY_DLLIMPORT
#   define EIGENPY_DLLEXPORT
#   define EIGENPY_DLLLOCAL
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef EIGENPY_STATIC
// If one is using the library statically, get rid of
// extra information and use standard explicit template
// instantiation keyword.
#  define EIGENPY_DLLAPI
#  define EIGENPY_LOCAL
#  define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION extern template
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export and
// define the right explicit template instantiation keyword.
#  ifdef eigenpy_EXPORTS
#   define EIGENPY_DLLAPI EIGENPY_DLLEXPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLEXPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLEXPORT
#  else
#   define EIGENPY_DLLAPI EIGENPY_DLLIMPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLAPI EIGENPY_EXPLICIT_INSTANTIATION_DECLARATION_DLLIMPORT
#   define EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLAPI EIGENPY_EXPLICIT_INSTANTIATION_DEFINITION_DLLIMPORT
#  endif // EIGENPY_EXPORTS
#  define EIGENPY_LOCAL EIGENPY_DLLLOCAL
# endif // EIGENPY_STATIC
#endif //! EIGENPY_CONFIG_HH
